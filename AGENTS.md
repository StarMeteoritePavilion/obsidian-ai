# AI 知识库维护规范

## 目标

本仓库用于持续积累 AI 相关知识，包括基础概念、模型、训练与优化、推理、提示词、Agent、评测、安全和工程实践。

知识库采用三层结构：

- `raw/`：同步保存原始资料，是事实来源。
- `wiki/`：保存经过综合、交叉引用并持续更新的知识页面。
- `AGENTS.md`：规定目录、格式和维护流程。

## 基本原则

1. 所有知识页面使用中文。
2. `raw/` 中的原始资料导入后不得修改；需要修订时新增版本并保留旧版本。
3. `wiki/` 由 LLM 维护，优先更新已有页面，不为同一概念重复建页。
4. 每项事实性结论必须能追溯到 `raw/` 中的精确资料。
5. 新资料与现有结论冲突时保留双方观点、适用条件和来源，不静默覆盖。
6. 严禁猜测文件名、路径、属性名、标签、标题或链接目标；先读取现有文件获取精确表述，仍无法确定时询问用户。
7. 不为尚未出现的内容预建目录、页面、模板或工具。

## 文件约定

### 原始资料

- 原始资料存放在 `raw/`。
- 文件名应准确表达资料标题；存在同名资料时使用来源或发布日期区分。
- Markdown、PDF、图片和数据文件均可作为原始资料。
- 知识页面使用 `[[raw/精确路径]]` 链接原始资料；存在页码、章节或标题时同时标明精确位置。

### 知识页面

普通知识页面存放在 `wiki/`，使用以下属性：

```yaml
---
title: 页面标题
tags:
  - AI/分类
---
```

只使用与页面内容直接相关的标签。分类标签从以下值中选择：

- `AI/基础概念`
- `AI/模型`
- `AI/训练与优化`
- `AI/推理`
- `AI/提示词`
- `AI/Agent`
- `AI/评测`
- `AI/安全`
- `AI/工程实践`

正文按实际内容组织，必要时使用以下章节：

- `## 核心结论`
- `## 机制`
- `## 适用条件与限制`
- `## 争议与变化`
- `## 相关概念`
- `## 来源`

没有实际内容的章节不得保留。内部概念使用 `[[页面标题]]` 链接；来源列表使用以下格式：

```markdown
- [[raw/精确路径]]：该资料支持或质疑的具体结论。
```

### 特殊页面

- `wiki/index.md` 是内容索引。新增、重命名或删除知识页面时必须同步更新。
- `wiki/log.md` 是追加式维护日志，不得改写历史记录。
- 日志标题统一为 `## [YYYY-MM-DD] 操作 | 标题`。
- 操作值只使用：`初始化`、`导入`、`查询`、`维护`、`查错`。

## 导入流程

默认每次处理一份原始资料：

1. 确认资料已存入 `raw/`，读取其精确路径和完整内容，不修改原文件。
2. 先读取 `wiki/index.md`，再搜索 `wiki/` 中与资料相关的现有页面。
3. 提取资料的主要结论、证据、适用条件、限制、新概念及与既有结论的冲突。
4. 优先更新已有知识页面；只有出现内容充分且边界清楚的新概念时才创建页面。
5. 补充相关页面之间的双向语义关联，并为事实性结论添加精确来源。
6. 更新 `wiki/index.md`。
7. 在 `wiki/log.md` 追加一条 `导入` 记录，列明原始资料及改动页面。
8. 检查未解析链接和无来源结论。

## 查询与回写

1. 查询时先读取 `wiki/index.md`，再定位相关知识页面。
2. 回答必须区分已有事实、来源中的不同观点和基于资料得出的综合判断。
3. 需要核实结论时读取对应原始资料，不依赖记忆补全。
4. 查询产生可复用且有来源支持的新结论时，将其写入现有页面或新建知识页面，并更新索引与日志。
5. 一次性回答、缺少来源的推测和重复内容不得写入知识库。

## 查错流程

定期检查以下问题：

- 页面之间相互矛盾但未说明来源、时间或适用条件。
- 已被新资料替代的陈旧结论。
- 未解析链接、孤立页面和缺少出链的页面。
- 已多次出现但没有独立页面的重要概念。
- 缺少原始资料支撑的事实性结论。
- `wiki/index.md` 与实际页面不一致。

完成检查后修复能够从现有资料中确认的问题，并在 `wiki/log.md` 追加一条 `查错` 记录。无法确认的问题必须明确记录并询问用户，不得自行补全。
