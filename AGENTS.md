# AI 知识库维护规范

## 核心原则
- `wiki/` 是持续更新、相互链接的知识成果，不是原始资料摘要的孤立集合。新资料和有长期价值的查询结论必须整合进已有页面，使综合结论持续反映全部已导入资料。
- 用户负责选择并确认事实来源、引导分析、提出问题和审阅结果；LLM 负责维护 `wiki/` 的摘要、交叉链接、归档、冲突标记、综合结论与一致性。
- 每项事实性结论必须能追溯到精确的原始资料。新旧资料冲突时保留各自观点、来源与适用条件，不静默覆盖。

## 知识库边界
- `raw/`：原始资料工作区，仅用于存放来源文件、关联资源和临时草稿。
- `raw/sources/`：原始资料层。资料经用户确认进入本目录后即成为不可变事实来源；LLM 只能读取，绝不修改。需要修订时新增版本并保留旧版本。
- `raw/assets/`：原始资料关联资源层。确认导入的图片、PDF、音视频等资源同样不可修改。
- `raw/inbox/`：临时草稿层。尚未确认的草稿放在这里，不作为事实来源；确认后迁移到 `raw/sources/`。
- `wiki/`：LLM 维护层。LLM 创建和更新结构化笔记、实体页、概念页、综合页、问题页和资料摘要页，用户浏览与审阅。
- `AGENTS.md`：知识库运行规范。只有当用户要求调整流程，或实际工作流已经稳定变化时才更新。
- 不把外部文章全文复制进 `wiki/`。除非用户明确要求保存原文，否则只写结构化摘要、综合结论、交叉链接和必要引用。

## 目录约定
- `wiki/index.md`：内容索引。按类别列出全部 wiki 页面，每项包含 wikilink 和一句摘要；每次新增、更新、重命名或删除 wiki 页面后同步更新。
- `wiki/log.md`：时间日志。按发生顺序在文件末尾追加记录，不重写历史。
- `wiki/sources/`：资料摘要页。
- `wiki/entities/`：人物、组织、项目、产品等实体页。
- `wiki/concepts/`：概念、方法、框架、术语页。
- `wiki/syntheses/`：跨资料综合、长期论点、对比分析页。
- `wiki/questions/`：值得沉淀的提问、分析结果和后续研究问题。
- 上述内容目录按实际需要创建，不预建空目录、页面或模板。

## Frontmatter 约定

- `raw/sources/` 原始文章依次使用 `title`、`source`、`author`、`created`、`tags`；`created` 记录来源发布日期。
- `wiki/sources/` 资料摘要依次使用 `title`、`source`、`author`、`published`、`ingested`、`updated`、`tags`。
- `published` 记录来源发布日期，`ingested` 记录资料进入知识库的日期，`updated` 记录摘要最近一次修改日期。
- 资料摘要不得使用 `created` 代替 `published`；新增或更新摘要时保持字段名称和顺序一致。

## 工作流

### Ingest
1. 确认资料已由用户放入或确认迁入 `raw/sources/`；读取完整内容，不修改原文件。一次只处理一个资料，除非用户明确要求批量处理。
2. 先读取 `wiki/index.md` 和相关页面，再读取新资料。
3. 提取主要结论、证据、适用条件、限制、新概念及与既有结论的冲突，新建或更新 `wiki/sources/` 下的资料摘要页。
4. 更新相关实体页、概念页、综合页或问题页，把新知识整合进现有 wiki；不得只留下孤立摘要。
5. 为事实性结论添加精确来源；出现冲突时在相关页面中标明各自来源、时间与适用条件。
6. 更新 `wiki/index.md`。
7. 在 `wiki/log.md` 文件末尾追加一条 `ingest` 记录。
8. 检查未解析链接、缺少来源的结论和受影响页面的一致性。

### Query
1. 先读 `wiki/index.md`。
2. 再读与问题直接相关的 wiki 页面。
3. 基于 wiki 综合回答并引用 vault 内部 wikilink；需要核实证据时再读取对应原始资料，不依赖记忆补全。
4. 区分资料中的事实、不同来源的观点和基于资料得出的综合判断。
5. 只有当用户要求保存，或明确属于知识库维护任务时，才把有长期价值且有来源支持的回答写入现有页面或新页面，并同步更新索引。
6. 在 `wiki/log.md` 文件末尾追加一条 `query` 记录；纯一般聊天不记入知识库日志。

### Lint
定期检查：
- 页面间未说明来源、时间或适用条件的矛盾。
- 孤立页面。
- 缺失反向链接的关键概念。
- 新资料已经修正但旧页面仍未更新的表述。
- 未解析链接和缺少出链的知识页面。
- 缺少原始资料支撑的事实性结论。
- 重复页面。
- 索引缺项。
- 日志格式异常。
- 现有资料无法回答、值得补充来源的知识缺口。

修复能够从现有资料中确认的问题，并在 `wiki/log.md` 文件末尾追加一条 `lint` 记录。无法确认的问题写入问题页并请求用户提供或确认来源，不得自行补全。

## 日志格式
`wiki/log.md` 的每条一级记录使用以下格式：

```markdown
## [YYYY-MM-DD] type | title
```

`type` 只使用稳定小写词：`init`、`ingest`、`query`、`lint`、`maintenance`。需要新增类型时先更新本规范。
