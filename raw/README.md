# 原始资料

本目录是原始资料工作区，按用途分为三个子目录：

- `sources/`：存放已确认导入的文章、论文和数据文件，是知识库的事实来源。
- `assets/`：存放已确认导入的图片、PDF、音视频等关联资源。
- `inbox/`：存放尚未确认的临时草稿；确认后迁移到 `sources/`。

- `sources/` 和 `assets/` 中的资料经用户确认后不得修改，LLM 只能读取。
- 资料需要修订时新增版本并保留旧版本。
- 知识页面必须通过精确路径链接到 `sources/` 中的资料。
- 本目录不存放总结、摘录、聊天记录或知识页面。
